# Dawn Of Stellar - 로그라이크 게임 의존성

# 필수 의존성
pygame>=2.0.0    # 사운드 시스템
numpy>=1.20.0    # 수학 연산

# 선택적 의존성 (향상된 기능)
colorama>=0.4.0      # Windows 컬러 터미널 출력
keyboard>=0.13.0     # 향상된 키보드 입력

# 개발/테스트 의존성 (CI/CD용)
pytest>=6.0.0       # 테스트 프레임워크
pytest-cov>=2.0.0   # 코드 커버리지
