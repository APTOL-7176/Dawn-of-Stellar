# Dawn Of Stellar - 로그라이크 게임 의존성 (2025.08.08 업데이트)

# 필수 의존성
pygame>=2.0.0    # 게임 엔진 및 사운드 시스템
colorama>=0.4.0  # Windows 컬러 터미널 출력
numpy>=1.20.0    # 수학 연산

# 웹/모바일 서버 (모바일 모드용)
flask>=2.0.0     # 웹 서버
flask-cors>=3.0.0 # CORS 지원
requests>=2.25.0  # HTTP 클라이언트

# 선택적 의존성 (향상된 기능)
keyboard>=0.13.0     # 향상된 키보드 입력 (선택사항)
pillow>=8.0.0        # 이미지 처리 (폰트 도구용)

# 빌드 도구
pyinstaller>=4.0     # 실행파일 빌드

# 개발/테스트 의존성 (선택사항)
pytest>=6.0.0       # 테스트 프레임워크
pytest-cov>=2.0.0   # 코드 커버리지
